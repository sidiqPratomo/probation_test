export interface OptionModel {
  value: string;
  label: string;
}

export const initialOption: OptionModel = {
  value: '0',
  label: 'label',
};

export const initialOptions: Array<OptionModel> = [];
